# Netlify: sitio estático. El build copia archivos a public/ para evitar 404.
[build]
  command = "mkdir -p public && cp index.html styles.css 404.html public/ && cp -r assets pages public/"
  publish = "public"
